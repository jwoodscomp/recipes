<% if obj.errors.any? %>
  <div class="row">
    <div class="alert alert-danger col-sm-12">
      <h5>Please correct these errors:</h5>
      <ul>
        <% obj.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>